# TO INSTALL TFBOOST:

# a local version of ROOT CERN is saved in the TFBoost folder
# the script automatically add a source .../thisroot.sh to the .bashrc file
# so that ROOT can be run typing $root in the terminal


1) OPEN A TERMINAL AND RUN:
   $ bash installRoot
   
   
2) INSTALL ALL DEPENDECIES
   $ bash packages
   
   
3) INSTALL TFBOOST
   $ bash TFBoost_installer
